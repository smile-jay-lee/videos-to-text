# Python依赖包列表

# Web框架
Flask>=2.3.0
Flask-CORS>=4.0.0
Werkzeug>=2.3.0

# 命令行媒体处理（系统依赖，非pip）：ffmpeg / ffprobe

# 语音识别
openai-whisper
# torch
torchaudio

# AI API
openai
requests
google-genai>=1.0.0
tqdm>=4.66.0

# 环境变量管理
python-dotenv>=1.0.0

# 工具库
numpy>=1.24.3,<2.0  # 限制版本以兼容 Whisper 的 numba 依赖

# 可选：GPU加速（如果有NVIDIA GPU）
# torch==2.0.1+cu118
# torchaudio==2.0.2+cu118
# 安装方式: pip install torch==2.0.1+cu118 torchaudio==2.0.2+cu118 -f https://download.pytorch.org/whl/torch_stable.html
