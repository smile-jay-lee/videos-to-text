# 🚀 快速启动指南

## 第一次使用

### 1. 安装FFmpeg（必需）

**Windows:**
```powershell
# 检查是否已安装
ffmpeg -version

# 如果未安装，使用Chocolatey安装
choco install ffmpeg

# 或手动下载并添加到PATH
# 下载地址: https://ffmpeg.org/download.html
```

### 2. 安装Python依赖

```powershell
pip install -r requirements.txt
```

### 3. 配置环境变量（可选）

```powershell
# 复制环境变量模板
copy .env.example .env

# 使用记事本编辑 .env 文件
notepad .env
```

如果需要AI功能（文本润色和总结），在`.env`中添加：
```
OPENAI_API_KEY=your-api-key-here
# 或
DEEPSEEK_API_KEY=your-api-key-here
```

### 4. 启动应用

```powershell
python main.py
```

### 5. 访问应用

打开浏览器访问：`http://localhost:5000`

---

## 使用流程

1. **上传文件** → 选择视频/音频文件
2. **选择设置** → 选择语言（中文/英文等）和模型大小
3. **开始转写** → 点击上传按钮
4. **等待处理** → 查看实时进度
5. **查看结果** → 自动跳转到结果页面
6. **下载文件** → 下载TXT格式文本

---

## 常用命令

### 查看日志
```powershell
# 查看今天的日志
type logs\app_20260108.log
```

### 清理临时文件
```powershell
# 删除上传的文件
Remove-Item "app\static\uploads\*" -Recurse -Force

# 删除输出文件
Remove-Item "outputs\*" -Recurse -Force
```

### 停止应用
在运行应用的终端按 `Ctrl+C`

---

## 故障排除

### FFmpeg未找到
**错误**：`FileNotFoundError: [WinError 2] 系统找不到指定的文件。`

**解决**：
1. 确认FFmpeg已安装：`ffmpeg -version`
2. 如果未安装，安装FFmpeg
3. 重启终端

### 端口被占用
**错误**：`Address already in use`

**解决**：
```powershell
# 查找占用5000端口的进程
netstat -ano | findstr :5000

# 结束进程（替换PID为实际进程ID）
taskkill /F /PID <PID>

# 或修改端口
# 在 .env 中设置：PORT=8000
```

### 内存不足
**错误**：`RuntimeError: CUDA out of memory` 或内存错误

**解决**：
- 使用更小的模型（在上传页面选择 tiny 或 base）
- 处理较小的文件
- 关闭其他占用内存的程序

### 模块未找到
**错误**：`ModuleNotFoundError: No module named 'xxx'`

**解决**：
```powershell
pip install -r requirements.txt
```

---

## 配置说明

### Whisper模型选择

| 模型 | 内存需求 | 速度 | 准确度 | 适用场景 |
|------|---------|------|--------|----------|
| tiny | ~1GB | 最快 | 一般 | 快速测试 |
| base | ~1GB | 快 | 良好 | **推荐日常使用** |
| small | ~2GB | 中等 | 很好 | 重要内容 |
| medium | ~5GB | 慢 | 优秀 | 高质量要求 |
| large | ~10GB | 很慢 | 最佳 | 专业场景 |

### 环境变量说明

```bash
# Flask配置
SECRET_KEY=your-secret-key          # 密钥（生产环境必改）
DEBUG=False                          # 调试模式
HOST=0.0.0.0                        # 监听地址
PORT=5000                           # 监听端口

# Whisper配置
WHISPER_MODEL=base                  # 默认模型

# AI配置（可选）
OPENAI_API_KEY=sk-xxx...           # OpenAI密钥
DEEPSEEK_API_KEY=sk-xxx...         # DeepSeek密钥
DEFAULT_AI_PROVIDER=openai         # 默认AI提供商
```

---

## 📚 项目文档

- 📄 [README.md](README.md) - 完整使用文档
- 📄 [项目分析文档.md](项目分析文档.md) - 项目分析
- 📄 [Python架构设计.md](Python架构设计.md) - 架构设计
- 📄 [项目整合完成总结.md](项目整合完成总结.md) - 完成总结

---

## 需要帮助？

1. 查看 [README.md](README.md) 中的常见问题部分
2. 查看日志文件：`logs/app_*.log`
3. 检查终端输出的错误信息

---

**🎉 祝使用愉快！**
