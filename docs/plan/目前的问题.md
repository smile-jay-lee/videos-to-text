1.数据直接通过内存传输，需要引入数据库，便于进行异步队列处理

2.js可能存在类型问题，ts可以有效避免

3.flask太原始了，改用fastapi框架

4.服务器内存不足，且无gpu，whisper流程需做出对应优化，在env中做区分，避免本地跑的时候受影响，复杂视频还是本地处理快一点。

5.ai未接入，提示词“这是一段语音转文本内容，其中可能存在因为语音识别错误导致的错别字，且没有合理的停顿，请整理一下这个文案，然后在开头加一个总结，中间是语音识别的文案内容修正，最后加一个对视频内容的评价，视频哪些观点相对客观，哪些内容可能有失偏颇，需要批判性思考”